/*

1.
it search for main() to execute the program
2.
yes
3.
yes, so that nobody initialized the object values outside that class.
4.
JVM creates the constructor because if there is no constructor that java provides the default constructor
5.
object created first and then constructor runs
first object is created and then constructor is run


 */
public class basics {
    public static void main(String[] args) {

    }
}
